@startuml
'https://plantuml.com/sequence-diagram

autonumber
actor CampusManager
CampusManager -> SPA : selects option to get passageways between buildings
activate SPA
CampusManager <- SPA : Displays the form to get insert needed information
deactivate SPA
CampusManager -> SPA :  clicks on load
activate SPA
SPA -> MGC: "api/passageway" -> GET
activate MGC

MGC -> MGC: Handles the body request,gets all the passageways Information
activate MGC
deactivate MGC
SPA <- MGC: Response status and Json
CampusManager <- SPA: Shows the response status and Json





@enduml
